<!--
  📄 문제 풀이 템플릿
  이 파일을 복사해 studies/week-XX/<깃허브ID>/<문제번호>-<문제이름>.md 로 저장하세요.
  아래 5요소는 반드시 모두 작성합니다. (접근/시간/공간/핵심/코드)
-->

# [SWEA 5658] [모의 SW 역량테스트] 보물상자 비밀번호

| 항목 | 내용 |
|:---|:---|
| 🔗 문제 | https://swexpertacademy.com/main/code/problem/problemDetail.do?contestProbId=AWXRUN9KfZ8DFAUo |
| 📚 주제 | 구현, 문자열 |
| 🎚️ 난이도 | 모의 SW 역량테스트 |
| ⏱️ 소요 시간 | 30분 |
| 🔁 다시 풀기 | 아니오 |

---

## 1. 접근 방법
<!-- 문제를 어떻게 이해했고, 왜 이 전략을 택했는지. 처음 떠올린 방법과 막혔던 지점도 좋음. -->
* 단순 구현 및 문자열 조작 유형으로 판단
* 한 변의 길이가 $N/4$이므로 $N/4$번 회전하면 모든 가능한 수 조합 추출
* `set`으로 중복 제거 후 내림차순 정렬하여 $K$번째 큰 수를 10진수로 변환


## 2. 시간 복잡도
<!-- 예: O(N log N) — 정렬 O(N log N) + 순회 O(N). 근거를 함께 적기. -->
<!-- SWEA는 시간 제한이 빡빡한 문제가 많으니 반드시 따져볼 것! -->

**O(N² log N)** —
* **회전 및 슬라이싱**: $N/4$회 회전 $\times$ 문자열 슬라이싱 = $O(N)$
* **정렬**: 최대 $N$개 수 내림차순 정렬 = $O(N \log N)$
* **10진수 변환**: $O(N)$

## 3. 공간 복잡도
<!-- 예: O(N) — 방문 배열 N개. -->

**O(N)** —
* 최대 $N$개 16진수 문자열 저장용 `set` 및 `list` 사용함.


## 4. 핵심 풀이 방법 ⭐
<!-- 이 문제의 결정적 아이디어 / 자주 하는 실수 / 알아야 할 개념. 가장 중요한 항목! -->
* **회전 주기성**: $N/4$번 회전 시 모든 16진수 수 추출
* **`set` 활용**: 중복 제거
* **10진수 변환**: 자릿수별 `16^p` 곱하여 합산


## 5. 실제 코드
<!-- 통과한 코드. 언어 태그를 정확히 표기하세요. -->

```python
T = int(input())

for test_case in range(1, T+1):
    N, K = map(int, input().split())
    input_str = input()

    rotated_nums = set()
    for i in range(N // 4):
        rotate_str = (input_str[-i:] + input_str[:-i])
        for j in range(0, len(rotate_str), N // 4):
            rotated_nums.add(rotate_str[j : j + N // 4])

    sorted_rotated_nums = sorted(list(rotated_nums), reverse=True)

    def hex_to_decimal(hex_str):
        ans = 0
        offset = 1
        for i in range(len(hex_str) - 1, -1, -1):
            c = hex_str[i]
            if c.isdigit():
                ans += int(c) * offset
            elif c.isalpha():
                ans += ((ord(c) - ord('A')) + 10) * offset
            offset *= 16
        return ans

    print(f"#{test_case} {hex_to_decimal(sorted_rotated_nums[K-1])}")
```

---

### 🧠 회고 (선택)
<!-- 배운 점, 비슷한 유형에서 재사용할 아이디어, 리뷰어에게 묻고 싶은 점 -->
* `ord()` 및 진법 변환 원리를 복습
* 파이썬 내장함수 `int(str, 16)`을 쓰면 더 간결히 풀 수 있다.
